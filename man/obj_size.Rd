% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/obj_size.R
\name{obj_size}
\alias{obj_size}
\title{Measure Object Size}
\usage{
obj_size(x, format = "MB", include_units = TRUE)
}
\arguments{
\item{x}{An R object whose size is to be measured.}

\item{format}{A character string specifying the size format.
Options are \code{"bytes"}, \code{"KB"}, \code{"MB"}, \code{"GB"}. Defaults to \code{"MB"}.}

\item{include_units}{Logical. If \code{TRUE}, appends the size units (e.g., MB, KB) to the output. Defaults to \code{TRUE}.}
}
\value{
A numeric value or a character string representing the size of the object in the specified format.
If \code{include_units = TRUE}, the result is a character string with units. If \code{FALSE}, only the numeric size is returned.
}
\description{
Computes the size of an R object in bytes, kilobytes, megabytes, or gigabytes.
}
\details{
The function uses base R's \code{object.size()} to compute the memory size of an object and converts it to the desired unit.
Units can be specified as \code{"bytes"}, \code{"KB"}, \code{"MB"}, or \code{"GB"}. The size is rounded to three decimal places for readability.
}
\examples{
# Default usage (size in MB with units)
obj_size(mtcars)

# Size in kilobytes
obj_size(mtcars, format = "KB")

# Size in bytes
obj_size(mtcars, format = "bytes")

# Size in gigabytes without units
obj_size(mtcars, format = "GB", include_units = FALSE)

# Invalid format defaults to bytes with a warning
obj_size(mtcars, format = "invalid_format")

}
