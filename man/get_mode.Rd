% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_mode.R
\name{get_mode}
\alias{get_mode}
\title{Compute the mode (most frequent value)}
\usage{
get_mode(x, tie_method = c("first", "all"), na_rm = TRUE)
}
\arguments{
\item{x}{An atomic vector (numeric, character, factor, etc.). Lists and data
frames are not supported.}

\item{tie_method}{How to handle ties when multiple values share the highest
frequency. One of \code{"first"} (default) to return only the first
encountered mode, or \code{"all"} to return every mode as a vector.}

\item{na_rm}{Logical. Should \code{NA} values be removed before computation?
Defaults to \code{TRUE}.}
}
\value{
A vector containing the mode value(s). Returns \code{NA} if
\code{x} contains no non-\code{NA} values.
}
\description{
Calculates the statistical mode—the value(s) that occur most frequently—of an
atomic vector.
}
\examples{
get_mode(c(1, 2, 2, 3))            # -> 2
get_mode(c("a", "b", "b", "c", "c"), tie_method = "all") # -> c("b", "c")
get_mode(c(NA, 1, 1, NA, 2), na_rm = TRUE)

}
