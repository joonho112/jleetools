% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svygtsum_continuous.R
\name{svygtsum_continuous}
\alias{svygtsum_continuous}
\title{Summarise a Continuous Variable (Unweighted vs Survey-Weighted)}
\usage{
svygtsum_continuous(
  design,
  by_var,
  cont_var,
  cont_label = NULL,
  caption = NULL,
  p_digits = 2,
  spanner_unwt = "Unweighted Sample",
  spanner_wt = "Survey Weight-Adjusted",
  statistic_fmt = c("{mean} ({sd})", "{median} ({p25}, {p75})", "{min}, {max}"),
  missing = "no"
)
}
\arguments{
\item{design}{A \code{survey.design} or \code{svyrep.design} object.}

\item{by_var}{Grouping variable to form columns (quoted or unquoted).}

\item{cont_var}{Continuous variable to summarise (quoted or unquoted).}

\item{cont_label}{Optional display label for \code{cont_var}. Defaults to the
variable name.}

\item{caption}{Optional table caption (Markdown is supported).}

\item{p_digits}{Integer; number of decimal places for \emph{p-values} (default
\code{2}).}

\item{spanner_unwt}{Title to place above the unweighted columns.  Default
\code{"Unweighted Sample"}.}

\item{spanner_wt}{Title to place above the weighted columns.  Default
\code{"Survey Weight-Adjusted"}.}

\item{statistic_fmt}{Character vector defining one or more summary lines,
in \strong{gtsummary} glue syntax.  Default shows mean (±sd), median (p25, p75),
and min, max:

\if{html}{\out{<div class="sourceCode">}}\preformatted{c("\{mean\} (\{sd\})",
  "\{median\} (\{p25\}, \{p75\})",
  "\{min\}, \{max\}")
}\if{html}{\out{</div>}}}

\item{missing}{Passed through to \strong{gtsummary}’s \verb{missing =} argument
(\code{"no"}, \code{"ifany"}, or \code{"always"}).  Defaults to \code{"no"}.}
}
\value{
A \code{gtsummary::tbl_merge} object.
}
\description{
Builds two \strong{gtsummary} tables for a \emph{continuous} variable—one using raw
(unweighted) statistics and one accounting for survey weights—then merges
them side-by-side with column spanners so differences are immediately
visible.  The interface and styling mirror
\code{\link[=svygtsum_crosstab]{svygtsum_crosstab()}} for categorical data.
}
\section{Notes}{

\itemize{
\item Rows with \code{NA} in either \code{by_var} or \code{cont_var} are dropped \strong{after}
conversion to \code{srvyr::survey_tbl}, so survey design information is
preserved.
\item P-values are formatted with \code{\link[gtsummary:style_pvalue]{gtsummary::style_pvalue()}} to \code{p_digits}
decimals.
}
}

\examples{
\dontrun{
tbl_temp <- svygtsum_continuous(
  design     = df_svy,
  by_var     = program_type,
  cont_var   = HB9_CESD7_TOT,
  cont_label = "CES-D-7 Total Score (0–21)",
  caption    = "CES-D-7 score by program type — unweighted vs weighted"
)
}

}
