% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/str_split_one.R
\name{str_split_one}
\alias{str_split_one}
\title{Split String into Components}
\usage{
str_split_one(string, pattern, n = Inf)
}
\arguments{
\item{string}{A character string to be split.}

\item{pattern}{A character string or regular expression that serves as the delimiter for splitting \code{string}.}

\item{n}{An integer specifying the maximum number of splits. Default is \code{Inf} (no limit on splits).}
}
\value{
A character vector containing the components of the string split by \code{pattern}. If the input string
results in no splits, an empty string (\code{""}) is returned.
}
\description{
A simple wrapper around \code{stringr::str_split} to return the first element of the split result.
Useful when only a single split result is needed without the outer list structure.
}
\examples{
# Split a string by a space
str_split_one("Hello World", pattern = " ")

# Split a file path by slashes
str_split_one("/usr/local/bin", pattern = "/")

# Split a CSV string
str_split_one("a,b,c", pattern = ",")

# Handle an empty string
str_split_one("", pattern = ",")

# Limit the number of splits
str_split_one("a,b,c", pattern = ",", n = 2)
}
