% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_triple.R
\name{save_triple}
\alias{save_triple}
\title{Save Data Frame to Multiple File Formats}
\usage{
save_triple(df, file_name)
}
\arguments{
\item{df}{A data frame or tibble to be saved.}

\item{file_name}{A base file name (without extension) to save the files. If an extension is included,
it will be replaced with \code{.rds}, \code{.dta}, and \code{.xlsx}.}
}
\value{
Invisible \code{NULL}. Files are written to the working directory or specified path.
}
\description{
\code{save_triple} saves a data frame to three different file formats: \code{.rds}, \code{.dta}, and \code{.xlsx}.
This function ensures that column names are sanitized for compatibility across formats
and handles errors gracefully for each format.
}
\details{
\itemize{
\item \strong{RDS}: Saves the data frame in R's native serialized format using \code{readr::write_rds()}.
\item \strong{DTA}: Saves the data frame in Stata's \code{.dta} format using \code{haven::write_dta()}.
\item \strong{XLSX}: Saves the data frame in Excel format using \code{writexl::write_xlsx()}.
}

Column names are automatically sanitized by replacing invalid characters (non-alphanumeric or underscores)
with underscores (\verb{_}) for compatibility across all file formats.
}
\examples{
\dontrun{
df <- data.frame(
  id = 1:5,
  name = c("Alice", "Bob", "Charlie", "Diana", "Eve"),
  value = c(10.5, 20.3, 30.1, 40.2, 50.4)
)

# Save to multiple formats
save_triple(df, "example_data")
}

}
