% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save_gtsummary_md.R
\name{save_gtsummary_md}
\alias{save_gtsummary_md}
\title{Save a \strong{gtsummary} Table as a Markdown (\code{.md}) File}
\usage{
save_gtsummary_md(tbl, file = "table.md", ...)
}
\arguments{
\item{tbl}{A \code{gtsummary} table (e.g.\ \code{tbl_summary}, \code{tbl_regression},
\code{tbl_merge}, …).}

\item{file}{File path for the Markdown file. Default \code{"table.md"}.  The
function ensures the path ends with the \code{.md} extension.}

\item{...}{Additional arguments passed to
\code{\link[gtsummary:as_kable_extra]{gtsummary::as_kable_extra()}}, such as \code{booktabs = TRUE},
\code{caption = "My table"}\ .}
}
\value{
(Invisibly) the normalized path to the written file, \strong{invisibly}.
A message noting the save location is printed for convenience.
}
\description{
Converts a \code{gtsummary} table to Markdown via
\code{\link[gtsummary:as_kable_extra]{as_kable_extra()}} and writes it to disk so the
table can be embedded in README files, Quarto/R Markdown documents, or issue
comments.  If the output directory does \strong{not} yet exist it is created
recursively.
}
\section{Error-handling}{

\itemize{
\item Checks that \strong{gtsummary} \emph{and} \strong{kableExtra} are installed.
\item Verifies \code{tbl} inherits from class \code{"gtsummary"}.
\item Ensures the output directory exists or creates it.
}
}

\examples{
\dontrun{
library(gtsummary)
tbl <- tbl_summary(iris, by = Species)

# Save with default name in working directory
save_gtsummary_md(tbl)

# Save to a docs/ folder with custom kableExtra options
save_gtsummary_md(
  tbl,
  file = "docs/iris_table.md",
  booktabs = TRUE,
  caption  = "Table 1. Iris summary"
)
}

}
